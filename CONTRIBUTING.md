# 协作开发指南

## 开发环境

- Node.js 22.13 或更高版本
- pnpm 11
- MySQL 8

## 本地启动

1. 复制 `.env.example` 为 `.env.local`，仅在本机填写真实配置。
2. 执行 `pnpm install`。
3. 按需执行数据库迁移，再运行 `pnpm dev`。

任何真实密码、AccessKey、短信密钥、生产数据或 `.env.local` 都不得提交到 GitHub。

## 分支与合并

- 从主分支创建 `feature/功能名` 或 `fix/问题名` 分支。
- 每个合并请求只解决一个清晰问题，并说明数据库和权限影响。
- 合并前至少运行：`pnpm lint`、`pnpm test`、`pnpm build:aliyun`。
- 涉及价格、付款、BOM、供应商、权限或审批的改动，必须同时验证职责分离和操作日志。

## 数据库变更

- 通过 Drizzle schema 和迁移文件管理结构变更。
- 禁止直接提交生产库导出或包含真实业务数据的 SQL。
- 迁移必须同时提供验证与回滚说明，并先在测试环境验证。

## 提交前安全检查

- 确认没有 `.env*`、私钥、令牌、手机号清单、发票附件或生产备份。
- 外部工厂和供应商的权限必须限定在所属组织和业务范围内。
- 高风险操作不得绕过手机验证码及原有双人审批。
