# 安全说明

本项目包含供应链、库存、质检、价格、发票和付款等敏感业务逻辑，建议始终使用 GitHub 私有仓库。

## 禁止提交

- 生产环境 `.env` 文件、数据库密码、AccessKey、API Key、短信密钥和私钥
- 生产数据库导出、备份、日志、发票、质检图片和用户上传附件
- `node_modules`、`.next`、缓存目录及历史部署压缩包

## 漏洞与泄露处理

请私下通知系统管理员和供应链负责人，不要在公开 Issue 中披露漏洞或凭据。若凭据曾进入 Git 历史，应立即停用并轮换，而不是只删除当前文件。

## 发布要求

AI 或开发者完成的代码修改，仍须由系统管理员与供应链负责人共同审批后发布。付款、价格、工厂更换、权限和预警解除等流程不得因为自动化而降低原有验证与审批要求。
